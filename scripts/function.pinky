func factorial(a, b, c, d, e, f)
    mul := 1
    for 1 := 1, n do
        mul := mul * i
    end
    if mul == 1 then
        println "The factorial of " + n + " is " + mul
    end
end

factorial(2)
